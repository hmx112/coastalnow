name: Verify Fishing alert details

on:
  push:
    branches:
      - fix/fishing-nws-alert-details-2

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: python src/test_fishing_alert_details.py
